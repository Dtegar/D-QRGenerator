import React from 'react';
import { motion } from 'framer-motion';
import Button from '../components/Button';
import Image from 'next/image';

const HeroSection = () => {
  return (
    <section id="hero" className="relative min-h-screen flex items-center pt-16 bg-teal-800">
      {/* Background overlay gradient */}
      <div className="absolute inset-0 bg-gradient-to-r from-teal-900/90 to-teal-800/70 z-10"></div>
      
      {/* Background image - will be replaced with actual image */}
      <div className="absolute inset-0 bg-[url('/placeholder-hero.jpg')] bg-cover bg-center z-0"></div>
      
      <div className="container mx-auto px-4 relative z-20">
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
          <motion.div
            initial={{ opacity: 0, x: -50 }}
            animate={{ opacity: 1, x: 0 }}
            transition={{ duration: 0.8 }}
          >
            <h4 className="text-orange-400 font-bold mb-4">Jasa Derek & Rental Alat Berat</h4>
            <h1 className="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-6">
              Solusi Terpercaya untuk Kebutuhan Transportasi & Konstruksi Anda
            </h1>
            <p className="text-gray-200 text-lg mb-8 max-w-lg">
              Aneka Perkasa menyediakan layanan derek towing dan rental alat berat berkualitas dengan harga terjangkau dan pelayanan profesional 24/7.
            </p>
            <div className="flex flex-wrap gap-4">
              <Button to="services" variant="primary">
                Layanan Kami
              </Button>
              <Button to="contact" variant="outline">
                Hubungi Kami
              </Button>
            </div>
          </motion.div>
          
          <motion.div
            initial={{ opacity: 0, scale: 0.8 }}
            animate={{ opacity: 1, scale: 1 }}
            transition={{ duration: 0.8, delay: 0.2 }}
            className="hidden lg:block"
          >
            {/* Placeholder for hero image - will be replaced with actual image */}
            <div className="rounded-lg h-96 w-full overflow-hidden relative">
              {/* Image will be placed here */}
              <Image 
                src="/images/sections/hero2_alatberat.jpg" 
                alt="Alat Berat Aneka Perkasa" 
                fill 
                className="object-cover rounded-lg"
                priority
              />
            </div>
          </motion.div>
        </div>
      </div>
    </section>
  );
};

export default HeroSection;